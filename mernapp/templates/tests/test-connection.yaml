apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mernapp.fullname" . }}-test-connection"
  labels:
    {{- include "mernapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mernapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
